version: '3'
services:
  mysql:
    image: mysql:5.7
    ports:
      - "3306:3306"

  springboot-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - mysql

